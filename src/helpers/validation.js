'use strict'

const validator = require('validator')

exports.validation = async (data) => {
    /*const val = validator.isEmpty(data)
    if (!val) throw new Error('Preencha todos os campos')
    return next()*/
}
